class AppConstants {
  static const String storageKeyTasks = 'tasks_storage_v1';
}

enum TaskStatus { todo, inProgress, done }

enum TaskPriority { low, medium, high }

const String AppTitle = 'TaskManagementApp';

